# API配置示例
# 如果前端部署在CDN，API部署在其他服务器，请配置此变量
# 示例：VITE_API_BASE_URL=https://api.example.com/v1
VITE_API_BASE_URL=

# 注意：
# 1. 如果不设置或留空，默认使用相对路径 '/v1'（前后端同域部署）
# 2. 也可以在 index.html 中通过 window.__API_BASE_URL__ 动态配置
